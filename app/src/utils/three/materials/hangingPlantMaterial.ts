import * as THREE from "three";

export const hangingPlantMaterial = new THREE.MeshPhysicalMaterial({
    color: 0x498644,
    roughness: 0.9,
    metalness: 0.0,
    transmission: 0.0,
    thickness: 0.0,
    clearcoat: 0.3,
    clearcoatRoughness: 0.7,
});